using UnityEngine;
using System.Collections;

public class SingleNote : Note {

    public override void Initialize(double judgeTime, double bpmTemp, double apperTime, int line)
    {
        base.Initialize(judgeTime, bpmTemp, apperTime, line);
    }
}
